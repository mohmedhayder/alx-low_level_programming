#include <stdio.h>
/**
 * main - prints the program name
 * @argc: number of args
 * @argv: array of args
 * Return: Always 0.
*/

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
